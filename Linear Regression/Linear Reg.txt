Linear Regression 
-> Solves Regression problem 
-> Have to find Best Fit Line
-> Uses MSE (Mean Squared Error)
-> Uses Gradient Descent = to reach the global minima point, using convergence theorem

#Evaluation Metrics :- These are the metrics how we measure how good a model is.
1. Mean Absolute Error (MAE)
2. Mean Squared Error (MSE)
3. Root Mean Squared Error (RMSE)
4. R-squared
5. Adjusted R-squared

#Feature Engineering :- Transforming, creating, or selecting input variables to make our model perform better.
-> Encoding Categorical variables

drop_first=True :== Solves the 'Dummy Variable trap', multi-collinearity.
                    Removes one redundant variable.

-> Create Useful derived features
-> Interaction Features - creating features in our dataset which would increase the overall accuracy.
                        like bmi_smoker or age_smoker

-> Scaling Numeric Features - Normalization and Standardization
-> Feature Selection


#Overfitting - means model is too complex
training - 95% ---- Low bias (bias means error from wrong assumptions)
testing - 65% ---- High variance (variance means sensitivity to small data changes)

#Underfitting - means model is too simple
training - 70% ---- High bias
testing - 65% ---- Low bias
